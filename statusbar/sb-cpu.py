#!/usr/bin/env python
import requests
import psutil

# one of those fzf process killers in the works
#processes = psutil.pids()
#for process in processes:
#    p = psutil.Process(process)
#    print(p.name())
